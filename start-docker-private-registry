#!/usr/bin/env bash

set -o nounset
set -o errexit
set -o pipefail

kubectl apply -f docker-private-registry.json -n default
echo "waiting before starting proxy..." && sleep 5
kubectl apply -f docker-private-registry-proxy.json -n default

